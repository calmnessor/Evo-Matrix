# 3D 视觉与点云

> VLA 的 Vision 不应止于 2D——机器人在 3D 世界中行动，必须理解深度、空间关系和 3D 结构。

## 为什么具身智能需要 3D 视觉？

2D 视觉告诉你"图像里有一只杯子"，但机器人需要知道："杯子在我前方 0.5 米处，杯口朝上，距离桌沿 3cm"。这些是 **2D 图像无法直接提供** 的信息：

| 任务 | 2D 能做的 | 必须 3D 才能做的 |
|------|----------|----------------|
| 抓取杯子 | 识别杯子类别 | 估计杯柄的 6-DoF 抓取位姿 |
| 推开障碍物 | 检测障碍物存在 | 判断障碍物在物体前方还是后方 |
| 插拔 USB | 识别 USB 口 | 对齐插头的 3D 朝向和深度 |
| 叠放物品 | 检测物体 | 理解物体的 3D 体积和堆叠关系 |

**一句话**：2D 负责"是什么"，3D 负责"在哪里、怎么操作"。

---

## 1. 机器人怎么"看"3D 世界？——传感器

### 1.1 深度相机 (RGB-D)

| 传感器 | 原理 | 精度 | 典型距离 | 缺点 |
|--------|------|------|----------|------|
| **RealSense D435** | 主动立体红外 | mm 级 | 0.1-10m | 阳光下失效，透明物体不行 |
| **Kinect v2** | ToF (飞行时间) | cm 级 | 0.5-4.5m | 已停产，但学术界还在用 |
| **ZED 2** | 被动双目立体 | cm 级 | 0.2-20m | 依赖纹理，低纹理场景差 |
| **iPhone LiDAR** | dToF | mm-cm 级 | 0.1-5m | 范围有限 |

### 1.2 LiDAR 与多视角

- **机械式 LiDAR**：360° 扫描，自动驾驶标配
- **固态 LiDAR**：非重复扫描，更便宜，机器人领域增长快
- **多视角 RGB**：Structure from Motion (SfM) / Multi-View Stereo (MVS) 重建 3D

> **课题组常用**：RealSense D435（桌面操作）+ ZED 2（移动操作）。仿真中直接用 MuJoCo/IsaacSim 的渲染深度图。

---

## 2. 核心 3D 表示

### 2.1 点云 (Point Cloud)

- **定义**：无序点集 $\{(x_i, y_i, z_i, f_i)\}$，$f_i$ 可以是 RGB、法向量、强度等
- **三大特性**：
  1. **无序性**：点云是集合而非序列
  2. **稀疏性**：大部分空间是空的
  3. **旋转不变性需求**：换个角度坐标完全不同但语义应一致

| 优点 | 缺点 |
|------|------|
| 原始数据，无信息损失 | 无拓扑结构，邻域搜索需 KNN/半径查询 |
| 存储高效 | 采样密度不均匀 |
| 直接反映传感器输出 | 缺少面/体积信息 |

### 2.2 深度图 (Depth Map)

- 每个像素值 = 该点到相机的距离，本质是 2.5D
- 优点：2D 卷积可直接用，与 RGB 天然对齐
- 缺点：遮挡区域无信息

### 2.3 体素 (Voxel Grid)

- 3D 空间的规则网格，每个格子存"是否被占据"或特征
- 致命缺点：内存 $O(n^3)$
- 缓解方案：稀疏体素 / 八叉树

### 2.4 TSDF / Occupancy

- **TSDF**：空间中每个点的值 = 到最近表面的距离（截断），零值面 = 物体表面
- **Occupancy Grid**：每格存 {occupied, free, unknown}，导航大量使用

### 2.5 隐式表示 (Implicit)

- **NeRF**：$f(x,y,z,\theta,\phi) \rightarrow (RGB, \sigma)$
- **Occupancy Network**：$f(x,y,z) \rightarrow [0,1]$
- 详见 [[NeRF神经辐射场]]

### 2.6 3D Gaussian Splatting

- 用数百万个 3D 高斯椭球表示场景
- **核心优势**：实时渲染 (100+ FPS)，显式表示方便编辑
- 详见 [[3DGaussianSplatting]]

### 选型速查

| 场景 | 推荐表示 |
|------|---------|
| 实时避障 / 导航 | 体素 / OctoMap |
| 精细操作 (抓取) | 点云 + 法向量 |
| 场景重建 / 新视角合成 | NeRF / 3DGS |
| 多传感器融合 | TSDF (如 KinectFusion) |

---

## 3. 点云处理必备操作

### 3.1 预处理

- **降采样**：体素网格滤波器 (VoxelGrid)、最远点采样 (FPS)、随机采样
- **去噪**：统计滤波 (Statistical Outlier Removal)、半径滤波
- **法向量估计**：每个点取 K 近邻拟合平面，法向量 = 平面法向

### 3.2 配准 (Registration) — ICP 算法

```
Repeat:
  1. 对 A 中每个点，找 B 中最近点（对应关系）
  2. 计算最优刚体变换 (R, t) 最小化对应点距离
  3. A ← R*A + t
Until 收敛
```

**实际使用**：先 RANSAC + FPFH 特征匹配做粗配准，再 ICP 做精配准。

### 3.3 分割与检测

- **语义分割**：每个点打类别标签 → PointNet++
- **实例分割**：区分同类别不同个体 → Mask3D, SAM-3D
- **6-DoF 位姿估计**：DenseFusion (RGB+点云融合), FoundationPose (2024 SOTA)

---

## 4. 点云深度学习

### 4.1 PointNet (CVPR 2017) — 开山之作

**核心思想**：用一个对称函数 (Max Pooling) 解决点云的无序性。

```
输入: N×3 点云
  ↓
T-Net (3×3) —— 学习旋转矩阵，对齐点云
  ↓
MLP (3→64→64) —— 逐点升维
  ↓
T-Net (64×64) —— 特征空间对齐
  ↓
MLP (64→128→1024) —— 逐点升维
  ↓
Max Pooling (对称函数) —— ∀顺序 → 同一个全局特征向量
  ↓
MLP (1024→512→256→K) —— 分类/分割输出
```

**关键方程**：$f(\{x_1, ..., x_n\}) \approx g(h(x_1), ..., h(x_n))$

其中 $h$ 是 MLP（逐点特征提取），$g$ 是对称函数（Max Pooling）。

**局限**：只提取全局特征，缺乏局部结构建模。

### 4.2 PointNet++ (NeurIPS 2017) — 层次化特征

```
PointNet++ 多层:
  1. Sampling (FPS 选中心点)
  2. Grouping (Ball Query 取邻域点)
  3. PointNet on each group → 局部特征
  ↓
  重复 1-3（抽象层级递增）
  ↓
  全局 PointNet → 最终特征
```

- **FPS (Farthest Point Sampling)**：均匀覆盖点云，$O(N \cdot n_{centers})$
- **Ball Query**：以每个中心点为圆心，半径 $r$ 内的所有点组成一个 group
- **MSG/MRG**：多尺度/多分辨率分组应对密度不均

| | PointNet | PointNet++ |
|------|----------|------------|
| 特征层次 | 只有全局 | 局部 → 全局 (多尺度) |
| 局部结构 | 忽略 | Ball Query 显式建模 |
| 密度适应性 | 差 | MSG/MRG 处理 |
| 适合任务 | 分类 | 分割、检测、抓取位姿 |

### 4.3 后续重要工作

- **DGCNN (2019)**：EdgeConv 动态构建 KNN 图
- **Point Transformer (2021)**：自注意力引入点云
- **实战提示**：分类用 1024 点，分割用 4096 点，抓取位姿预测用 2048-4096 点

---

## 5. 3D 视觉在 VLA 中的实际应用

### 5.1 当前范式 vs 3D 范式

```
当前 VLA (2D 为主):
  摄像头 → 2D ViT/SigLIP → 语言指令 → Action Head → 末端位姿
  问题: "前推杯子" 和 "避开杯子" 在 2D 特征里可能很相似

加入 3D 后:
  多视角 RGB / 深度图 → 3D Backbone → 3D 特征
                    ↘            ↙
                  2D 特征 + 3D 特征 融合
                    ↓
              Action Head → 6-DoF 动作
  优势: 明确知道物体的空间位置和表面几何
```

### 5.2 关键论文导读

| 论文 | 3D 用法 | 经验 |
|------|---------|------|
| **3D-VLA** (ICML 2024) | 3D 生成式世界模型 | 用 3D 表示统一预测未来状态和动作 |
| **DP3** (3D Diffusion Policy) | 稀疏点云 → 扩散策略 | 点云输入 + 扩散去噪预测动作序列 |
| **3D Diffuser Actor** | 3D 点云 → 扩散 Transformer | 3D 场景 + 语言 → 多步动作 |
| **PerAct** | 体素化场景 → 动作分类 | 利用体素表示做 6-DoF 操作 |
| **RVT** | 多视角 2D → 3D 回归 | 在 2D 图像平面做推理再反投回 3D |

---

## 6. 入门代码示例（Open3D）

```python
import open3d as o3d
import numpy as np

# 1. 读取点云
pcd = o3d.io.read_point_cloud("bunny.ply")
print(f"点云有 {len(pcd.points)} 个点")

# 2. 降采样
pcd_down = pcd.voxel_down_sample(voxel_size=0.01)

# 3. 估计法向量
pcd_down.estimate_normals(search_param=o3d.geometry.KDTreeSearchParamHybrid(radius=0.02, max_nn=30))

# 4. 可视化
o3d.visualization.draw_geometries([pcd_down], point_show_normal=True)

# 5. ICP 配准示例
source = pcd_down
target = pcd_down.translate((0.1, 0, 0))  # 模拟位移
reg_p2p = o3d.pipelines.registration.registration_icp(
    source, target, max_correspondence_distance=0.02,
    init=np.eye(4),
    estimation_method=o3d.pipelines.registration.TransformationEstimationPointToPoint()
)
print(f"ICP 收敛: {reg_p2p.inlier_rmse:.6f}")
```

---

## 7. 新手学习路线

### 第一阶段：建立直觉（1-2 天）
1. 用 Open3D 可视化一个点云：`open3d.io.read_point_cloud("bunny.ply")`
2. 理解深度图、点云、体素的相互转换
3. 动手：把一张 RealSense 深度图转成点云

### 第二阶段：核心算法（3-5 天）
1. **PointNet**：读论文，跑官方代码，理解 Max Pooling 如何解决无序性
2. **PointNet++**：理解 FPS + Ball Query
3. **ICP 配准**：用 Open3D 把两个点云拼在一起

### 第三阶段：重建与 VLA 结合（5-7 天）
1. Nerfstudio: 用自己的手机拍一段视频 → 训练一个 NeRF
2. 了解 3DGS 为什么比 NeRF 快
3. 读 DP3 或 3D Diffuser Actor：理解 3D 点云如何进入扩散策略

---

## 8. 推荐资源

### 必读论文

| # | 论文 | 关键词 | 理由 |
|---|------|--------|------|
| 1 | PointNet (1612.00593) | 对称函数、T-Net | 入门必读 |
| 2 | PointNet++ (1706.02413) | FPS, Ball Query | 层次化思想的经典实现 |
| 3 | NeRF (2003.08934) | 体渲染、位置编码 | 理解隐式 3D 表示 |
| 4 | 3DGS (2308.04079) | 高斯泼溅、实时渲染 | 当前最热场景重建方法 |
| 5 | DP3 / 3D Diffuser Actor | 3D + 扩散策略 | 与 VLA 方向直接相关 |

### 教程与工具

- **[Open3D 官方教程](http://www.open3d.org/docs/release/tutorial/)**：点云处理全家桶
- **[Nerfstudio 文档](https://docs.nerf.studio/)**：一行命令训练 NeRF
- **[3D Gaussian Splatting 从零实现](https://papers-100-lines.medium.com/3d-gaussian-splatting-tutorial-from-scratch-in-100-lines-of-pytorch-code-no-cuda-no-c-6ef104dc6419)**：100 行 PyTorch 理解 3DGS

---

## 9. 自检问题

### 基础关
- [ ] 我能解释点云、深度图、体素三种表示的区别和适用场景
- [ ] 我理解点云的无序性及 PointNet 如何用对称函数解决
- [ ] 我知道 PointNet 和 PointNet++ 的核心区别
- [ ] 我能解释 NeRF 的基本原理
- [ ] 我理解 Gaussian Splatting 相比 NeRF 的核心优势

### 进阶关
- [ ] 我能解释 FPS 采样的工作原理
- [ ] 我理解 Ball Query 和 KNN 的区别
- [ ] 我知道 ICP 的迭代步骤及局限
- [ ] 我能说出 3D-VLA 与传统 2D VLA 的核心差异

### 实战关
- [ ] 我能用 Open3D 读取、降采样、可视化一个点云
- [ ] 我能跑通 PointNet 官方代码
- [ ] 我能用 Nerfstudio 从手机视频训练一个场景的 NeRF
- [ ] 我能说出课题组操作场景中 3D 视觉的典型使用链路

---

## 关联笔记

- [[NeRF神经辐射场]] — 隐式 3D 表示详解
- [[3DGaussianSplatting]] — 显式 3D 表示 + 实时渲染
- [[../计算机视觉与ViT/计算机视觉与ViT|计算机视觉与ViT]] — 2D 视觉基础
- [[../../02-VLA/扩散策略|扩散策略]] — 3D 特征 + 扩散策略
- [[../机器人运动学|机器人运动学]] — 6-DoF 位姿、坐标系变换

---

> **给新人的建议**：别试图一次性理解所有内容。先能跑起来——用 Open3D 可视化一个 bunny.ply，用 Nerfstudio 重建一个小场景。代码跑通之后回头读 PointNet 论文，你会发现很多设计不再抽象。
